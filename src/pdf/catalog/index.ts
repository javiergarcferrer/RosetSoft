// The LifestyleGarden catalog PDF (client-facing, in-stock pieces only).
// Loaded via safeDynamicImport from the Inventario · LifestyleGarden page (the
// @react-pdf bundle is heavy). Delivery reuses the shared blob pipeline.
export { generateLsgCatalogPdf } from './generate.js';
export { downloadBlob } from '../react/deliver.js';
